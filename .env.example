# 环境变量配置示例文件
# 复制此文件为 .env 并填入实际的值

# ============================================================================
# JWT 配置
# ============================================================================
# JWT 签名密钥（必须设置，生产环境使用强随机密钥）
# 生成方法: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=dev-secret-key-change-this

# JWT 算法（默认 HS256，无需修改）
JWT_ALGORITHM=HS256

# ============================================================================
# Flask 配置
# ============================================================================
# Flask 密钥（必须设置，用于 session 加密等）
# 生成方法: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=dev-flask-secret-change-this

# Flask 环境（development 或 production）
FLASK_ENV=development

# ============================================================================
# 数据库配置
# ============================================================================
# 数据库文件路径（可选，默认为项目目录下的 rongzao.db）
DB_PATH=/path/to/rongzao.db

# ============================================================================
# 日志配置
# ============================================================================
# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=INFO

# ============================================================================
# 服务器配置
# ============================================================================
# 服务器端口（可选，默认为 8818）
PORT=8818

# 服务器主机（可选，默认为 0.0.0.0）
HOST=0.0.0.0

# ============================================================================
# 生产环境特别说明
# ============================================================================
# 1. 生产环境必须设置 JWT_SECRET 和 SECRET_KEY
# 2. 使用强随机密钥，不要使用默认值
# 3. 不要将 .env 文件提交到版本控制系统
# 4. 定期更换密钥（建议每季度）
# 5. 将密钥妥善保存到密码管理器中